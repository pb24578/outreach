import styled from 'styled-components';
import { FlexColumn, FlexRow } from '../../shared/styles';

export const Container = styled(FlexRow)`
  height: 97.5vh;
`;

export const ContactContainer = styled(FlexColumn)`
  width: 40vw;
`;

export const MessageContainer = styled(FlexColumn)`
  width: 55vw;
`;
